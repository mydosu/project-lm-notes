1. **模式 B 网络架构落地（板子在外面）**：插件写队列 + 板子壳轮询 + Windows 仅 ICS 网桥——去 ws（页面 `/api/poll` 2s HTTP 轮询拉取即清空），插件 3 Web API（pending/sessions/ping，前缀 `/api/v1/plugins/extensions/live2d-kiosk/`），板子壳 board_shell.py 每 3s 拉队列控屏（详见 [[astrbot 插件开发]]）
2. **会话切换功能**：插件记 origin（`unified_msg_origin`）+ 壳按 astrbotSession 过滤 + 后台会话下拉（sessions API）；无 origin 消息全局显示
3. **插件加载 4 连修**：插件名必须合法 Python 标识符（live2d_kiosk）、`@event_message_type(EventMessageType.ALL)`、config 参数可选、LLM 钩子 `register_on_llm_response`（详见 [[astrbot 插件开发]]）
4. **LLM 回复上屏**：`MessageChain.message` 方法形态兼容 + result_chain 空 → **raw_completion 兜底**——屏幕显示助手自己的回复（speak_user_msg=false，不转发用户消息）
5. **测试连接 + 壳状态**：后台测试按钮（代理插件 ping）+ 状态区 10s 自动刷新
6. **气泡滞留可配**（bubbleHold 0=一直）+ 会话选择重启不失效（兜底 option）+ 保存后自动拉会话列表
7. **日期/时间独立布局**（date-display 移出 time-block 防 transform 联动）+ **气泡溢出修复**（`word-break: break-all` + `transform-origin: left top` + max-width 随缩放反比）
8. **清空屏幕消息**：插件 clear API + `/api/clear`（清插件队列 + 本地队列 + 页面恢复占位）
9. **单项重置按钮**：显示 7 项 + 排版 5 模块（X/Y/S 归位）+ 智能助手 3 项——一键重置保留
10. **后台去 emoji + CSS 美化**：装饰 emoji 全清（保留状态符号 ⚠✅❌🔒）、22px 圆角/hover 过渡/focus 光圈/select 自定义箭头/文件选择卡片化
11. **背景主题 10 个 + 自定义背景图**：canvas 4:3 裁剪上传（拖拽框/九宫格）、图片优先于主题、切换主题自动移除图片
12. **气泡半透明磨砂渐变**：后台可选基色 → 135° 渐变 + blur(8px)（实测不掉帧）
13. **每模块独立字体 + 8 款艺术字** + **中文适配**（圆润系配站酷快乐体/优雅系配小薇体，各 3838 字子集共 1.4MB）+ **切换闪现修复**（font-display block + document.fonts.load 预加载 + seq 防竞态）
14. **主仓库历史合并 + push**：远程为 force 重写历史（无共同祖先）→ `git merge --allow-unrelated-histories -X ours` 合并 22 个本地 commit 零冲突；README 架构更新为模式 B + 域名打码 + 清理 pyc
15. **Obsidian 开发笔记开源**：单开仓库 `mydosu/project-lm-notes`（PUBLIC，MIT）——副本打码（公网域名 `<板子DDNS域名>`、协助账号 `<远程协助账号>`），原库不动；.gitignore 排除 .obsidian
16. **Agent 接口文档 v2**：ws 时代 → 模式 B（插件队列 + 板子壳轮询）；服务表加 board-shell；API 表 +9 接口；发送方式改"插件队列主推 + 直连备选"（vault 主源 + 主仓库 docs + 开源副本三处同步）

### [[astrbot 插件开发]]

### [[Live2D Kiosk 排障记录]]

### [[Kiosk部署完成]]

### [[Agent接口文档]]
