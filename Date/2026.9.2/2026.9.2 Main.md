1. **后台代码重构（不破坏功能）**：抽 `api()` helper（统一 fetch+JSON）、`SAVE_FIELDS` 映射表替代 4 个保存分支、`SWITCH_FIELDS` 映射替代嵌套三元开关、`draftField()` 统一草稿写入、render 抽 `pair()` 三元组 helper——纯内部简化，33 项锚点断言全 PASS
2. **后台界面丝滑美化**：按钮流光扫过（::after）、卡片 hover 上浮、toast 弹性弹入（cubic-bezier 回弹）、侧栏顺滑过渡 + 导航 hover 左滑、细窄粉色滚动条
3. **天气不可用排查**：板子 DNS/路由正常但 wttr.in TCP 443 超时（国外站点不可达），国内免 key 源全不通——根因是**网络路径**（详见 [[Live2D Kiosk 排障记录]]）
4. **天气源可配置**：wttr 默认 + 高德降级；后台天气源下拉 + Key 输入；带超时 fetch（AbortController 6-8s）快速失败
5. **USB 联网模式重构（去 RNDIS 推送）**：确认 RNDIS 是底层驱动、ICS 是共享——USB 与 WiFi 并列**联网方式**（`infoSource: rndis`→`usb`），删除 externalClock/externalWeather/timeinfo 电脑推送逻辑，时间/天气/位置统一网络获取
6. **天气源默认改高德 + 双源降级**：amap 默认（城市名直查 + IP 定位兜底）、wttr 海外备选
7. **城市留空自动定位修复**：高德 /v3/ip 对 IPv6 出口返回空 → 新增 `/api/geoip` 后端代理（服务端请求 myip.ipip.net 解析省市，兼容 IPv6）——实测屏幕显示"贵阳市 · 22°C · 晴"
8. **后台 UI 精修（侧栏 + 细节）**：导航 5 项线性 SVG 图标（收起态不再空）、收起态 active 渐变粉胶囊底贴合图标（去掉整条宽底上下留白）、收缩按钮 ◀ 字符 → SVG chevron 180° 旋转过渡、键盘焦点光圈（:focus-visible）、开关/滑条/色块抓取反馈、dirty 保存按钮柔和呼吸、数值 tabular-nums、触达目标窄屏放大、prefers-reduced-motion 支持、dev-addr 旧地址 30.1 → 137.2 修复

### [[astrbot 插件开发]]

### [[Live2D Kiosk 排障记录]]

### [[Kiosk部署完成]]

### [[Agent接口文档]]
