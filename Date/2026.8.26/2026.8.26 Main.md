1. **性能/发热/开关机优化**：schedutil 调频（idle 降频降温）、开机无 logo（bootlogo=false + quiet，21.4s→17s）、关机加速（DefaultTimeoutStopSec=10s）、FPS 日志 10s→60s（省 SD 卡写入）
2. **OOM 卡死教训**：激进 Chromium flags（`--renderer-process-limit=1`）单渲染进程内存暴涨 → 系统瘫痪 → 撤回全部激进 flags + 恢复蓝牙
3. **内核崩溃修复（自动重启根因）**：schedutil 频繁调频触发 **H616 CCU 时钟驱动 bug**（内核 Oops: `ccu_div_set_rate` / Comm: `sugov:0`）→ 恢复 **performance** + 新增 `cpufreq-performance.service` 开机锁定（稳定优先）
4. **项目开源 GitHub**：`live2d-kiosk`（主仓库）+ `astrbot-live2d-kiosk`（插件独立仓库）双仓库，MIT 许可，代码"去 AI 化"（口语化 README + 分阶段提交历史）
5. **astrbot 插件（LLM 工具调用）**：注册 `live2d_emotion` / `live2d_action` / `live2d_speak` 三个工具——**大模型按自己回复的情感**自动控制屏幕表情/动作/气泡（v1.1.0，mock 验证 16 项 PASS，详见 [[astrbot 插件开发]]）

### [[astrbot 插件开发]]

### [[Live2D Kiosk 排障记录]]

### [[Agent接口文档]]

### [[Kiosk部署完成]]
