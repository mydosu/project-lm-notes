1. **清空屏幕消息修复（clear_ts 幂等版本号）**：`/api/poll` 拉取即清空导致 kiosk 与预览 iframe 竞争丢失 clear 广播——`/api/clear` 改更新 `CLEAR_TS` 时间戳，poll 返回它，两端各自比对执行清屏（无竞争）；同时修壳过滤条件：`if target and origin != target`（origin 为空也过滤，LLM 自动回复等消息 origin 提取不到时不再漏显示）
2. **蓝牙断开状态刷新**：后台状态轮询 15s → 3s + 页面切回立即刷新（visibilitychange）——手机断开后状态更快反映
3. **屏幕新增 WiFi / 蓝牙连接状态模块**：文字 + 自绘 SVG 图标（WiFi 三弧信号、蓝牙三叉，currentColor 跟随文字色，非 emoji）+ 状态圆点（在线彩色/离线灰）；5s 轮询 `/api/net/status`、`/api/bt/status`；后台全套配置：显示开关、字体风格、字体颜色、连接圆点颜色（dotColors）、布局滑条（X/Y/S）、预览拖拽/缩放
4. **预览拖拽误判缩放修复（四角判定）**：原来"距任一边 ≤10px → 缩放"，新模块高仅 ~21px 按中间也命中 → 改四角判定（dx 且 dy 都 ≤ EDGE 才缩放），小模块中心可移动、角上仍可缩放
5. **连接圆点颜色可配置**：`dotColors: {wifi, bt}` 配置链 + 后台颜色选择器（含单项重置），CSS 变量 `--dot-on` 注入（离线自动灰）
6. **WiFi/蓝牙布局滑条无坐标值修复**：admin.html 布局模块数组共 5 处，render() 与预览回传消息处理漏加 wifi/bt——补齐后滑条显示原始坐标（230/-195/1.38）
7. **pin 方案尝试并回滚**：为修复"隐藏 wifi/bt 后气泡错位"，尝试加载时按全部显示流位置把模块转绝对定位（pin）——但预览 iframe 环境测量异常导致模块全推出屏幕 → 回滚（详见 [[Live2D Kiosk 排障记录]]）
8. **flow-comp 流补偿引入**：改为不改变布局结构、显隐变化时测量流位置差叠加 translateY 补偿（保持视觉位置）——后续 9.5 因 iframe 测量 bug 也回滚（详见排障记录）

### [[Live2D Kiosk 排障记录]]

### [[Kiosk部署完成]]
