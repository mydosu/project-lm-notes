1. **flow-comp 基准锁定改进尝试**：基准改为首次锁定（占位文本测量），内容变化不漂移——但 iframe 环境测量 bug 仍未解决（详见排障）
2. **flow-comp 彻底回滚**：iframe 内部实测发现模块 transform 被加成天量补偿（时间 `translate(140px,-341px)`，top -161 全推出屏幕）——根因是 measureFlow 在 iframe 时序下量到错误流位置；回滚两个 flow-comp commit 回到干净版本，模块位置恢复正常（时间 40 / 日期 146 / 天气 193 / 气泡 241）
3. **预览拖拽命中区改绑虚线框**：时间/日期/天气是块级被 flex 拉伸到整行宽（630px），虚线框只贴文字（205px）——事件原绑元素导致框外空白也能拖动；改为绑在虚线框（pv-box）上（pointer-events: auto）——**所见即所得，只有框内可操作**
4. **预览光标修复**：元素上的 `cursor: move` 移除（块级拉伸区不再显示移动标识），只保留虚线框 move——框内 move / 框外默认箭头（板端实测通过）
5. **收尾 push**：36 个 commit 推送 GitHub（mydosu/live2d-kiosk），两仓库同步；清理全部临时文件（D:\CODE 60 个测试文件、板子临时脚本、Temp 无残留）

### [[Live2D Kiosk 排障记录]]

### [[Kiosk部署完成]]
