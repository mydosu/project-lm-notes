1. 完成 Live2D Kiosk **全功能部署**：开机自启 → Xorg → Chromium 全屏 → easy-live2d 模型（Hiyori 等 8 个）→ Mali-G31 GPU 硬件加速，**稳定 12fps**（keepalive 挂起 evaluate 方案）
2. 实现**全功能界面**：左侧面板（时间/天气/对话气泡）+ 右侧 Live2D 模型（画布自适应 + 布局 X/Y/缩放可调）
3. 完成**管理后台**（Flask :8080 + WebSocket :9000）：模型上传/切换、界面排版滑条、显示开关、WiFi 连接管理、信息源切换、自定义 ws 端口、保存/重置按钮
4. 打通 **agent 对接**：emotion/action/speak/timeinfo 消息控制表情/动作/气泡/时间天气（Haru/Mao 8 表情实测可用）
5. **产品化适配**：无网卡方案（WiFi 天线自启 + RNDIS 电脑推送双信息源）、屏幕常亮、开机自启（三服务 + WiFi 自动连接）
6. 板子只留功能文件（/opt/dashboard），开发文件保存在电脑本地，一键部署脚本自动化
7. **伪全息适配**：分光棱镜反射像上下颠倒 → `xrandr --rotate left --reflect y` 垂直翻转输出；管理后台改**白色+粉色清新风格**（测试通过）
8. **显示修复**：白屏+宽胖变形根因（reflect mode 漂移 768x1024 + 横屏 mode 比例相反）→ `--mode 480x640`（面板原生竖屏，rotate 后 4:3 与 viewport 等比）锁定
9. **后台升级**：可伸缩侧边栏分页（模型管理/网络与信息/显示设置/界面排版/智能助手）+ 全部滑条配数值输入 + 模型缩放迁移到界面排版
10. **后台美化**：毛玻璃（backdrop blur）+ 渐变背景 + 光斑图案 + 卡片自适应窗口 + 排版网格对齐
11. **显示细节**：气泡字体自适应+大小可调、各模块字体颜色可选、日期/星期独立控制（显示/布局/颜色）
12. **产品化收尾**：后台文案大众化（消费者向，去技术术语）+ RNDIS 网段改 **192.168.30.1**（避免与用户局域网冲突，电脑端 DHCP 免改）
13. **重启按钮 + 加载遮罩居中**：后台新增"🔄 重启设备"（POST /api/reboot）；loader 按屏幕尺寸定位居中（CDP/scrot 对比发现 viewport 与屏幕 1:1 映射）
14. **reboot 卡住根治**：live2d-kiosk.service 加 `TimeoutStopSec=10`（Chromium 不响应 SIGTERM，90s 等待 → 10s 强制 SIGKILL）
15. **模型大小持久化**：手动部署 `cp -r dist/.` 覆盖 config.json 导致用户设置丢失 → 必须走 deploy.py（自动备份恢复）

### [[Kiosk部署完成]]

### [[Agent接口文档]]

### [[Live2D Kiosk 排障记录]]
